apiVersion: apps/v1
kind: Deployment
metadata:
  name: authorization-service-deployment
  namespace: job-portal
  labels:
    app: authorization-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: authorization-service
  template:
    metadata:
      labels:
        app: authorization-service
    spec:
      containers:
      - name: authorization-service
        image: samarth00729/job-portal-authorization-service:latest
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: job-portal-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: authorization-service-service
  namespace: job-portal
spec:
  selector:
    app: authorization-service
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-service-deployment
  namespace: job-portal
  labels:
    app: job-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: job-service
  template:
    metadata:
      labels:
        app: job-service
    spec:
      containers:
      - name: job-service
        image: samarth00729/job-portal-job-service:latest
        ports:
        - containerPort: 8083
        envFrom:
        - configMapRef:
            name: job-portal-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: job-service-service
  namespace: job-portal
spec:
  selector:
    app: job-service
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP

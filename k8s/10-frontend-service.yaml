apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-service-deployment
  namespace: job-portal
  labels:
    app: frontend-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-service
  template:
    metadata:
      labels:
        app: frontend-service
    spec:
      containers:
      - name: frontend-service
        image: samarth00729/job-portal-frontend-service:latest
        ports:
        - containerPort: 8085
        envFrom:
        - configMapRef:
            name: job-portal-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service-service
  namespace: job-portal
spec:
  selector:
    app: frontend-service
  ports:
  - port: 8085
    targetPort: 8085
  type: ClusterIP
